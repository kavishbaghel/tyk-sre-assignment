apiVersion: v1
kind: Service
metadata:
  name: {{ include "tyk-are-assignment-app.fullname" . }}
  namespace: {{ include "tyk-are-assignment-app.namespace" . }}
  labels:
    {{- include "tyk-are-assignment-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}

  selector:
    {{- include "tyk-are-assignment-app.selectorLabels" . | nindent 4 }}

  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP